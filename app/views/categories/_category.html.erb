
    <tr id="<%= "#{dom_id(category)}_category_details" %>">
      <td class="txt-gerneral"><%= category.name %></td>
      <td class="txt-gerneral" id="<%= "#{dom_id(category)}_category_delete" %>"><%= link_to "Delete", category_path(category), data: { turbo_method: :delete }, class: "bttn-danger-sm" %></td>
    </tr>