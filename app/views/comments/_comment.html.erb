<% if comment.body.present? %>
  <div class="card">
    <div class="card-body">
      <%= render "comments/comment/comment_body", comment: comment %>
      <%= render "comments/comment/comment_footer", comment: comment %>
      <%= render "comments/comment/comment_replies", comment: comment %>
    </div>
  </div>
  <p></p>
<% end %>



